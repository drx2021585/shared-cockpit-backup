# GENERADO MECANICAMENTE por tools/generate_ifly_profile.py -- no editar a mano.
#
# Fuente: SimObjects/Airplanes/iFly 737-MAX8/model/iFly737Max_INTERIOR.xml del
# paquete iFly 737 MAX 8 v1.1.0.0 realmente instalado (copia de referencia de
# Darwin en apps/desktop-ui/recurso/ifly-aircraft-737max8/). Cada control sale
# del <Component> real: la L-Var de estado del <ANIM_CODE> y los codigos de
# comando del <CallbackCode>.
#
# Sistema: Sistema electrico
# Mecanismo de iFly (verificado leyendo el XML, no supuesto): la cabina NO tiene
# eventos H:/K:/B:. Un clic escribe un CODIGO ENTERO en la L-Var de trigger del
# sistema (L:VC_Electrical_trigger_VAL) y el WASM de iFly hace el resto,
# publicando el resultado en L:VC_<control>_VAL.
#
#   Lectura  : L:VC_<control>_VAL via FSUIPC7 (FsuipcLVarClient, area
#              "SharedCockpitBridge_LVars" -- 'field' es el nombre crudo de la
#              L-Var, que es exactamente lo que espera ReadLVar).
#   Escritura: calculator code RPN ejecutado por MSFSVariableServices de FSUIPC7
#              (write.type: calculatorCode, mecanismo ya confirmado en vivo para
#              el PMDG). El RPN compara el estado real contra el valor pedido y
#              solo entonces dispara el codigo -- por eso esto respeta la regla
#              anti-TOGGLE aunque iFly no exponga ningun SET absoluto.
#
# LIMITACION HONESTA: una escritura avanza UN paso hacia el destino. Para un
# selector de varias posiciones hacen falta varias escrituras (cada cambio de
# estado del otro piloto vuelve a disparar una). Ademas la POLARIDAD (que rueda
# arriba = valor mayor) se asume a partir de la convencion del XML y NO esta
# verificada en vivo: si en algun control resultara invertida, la escritura da
# un paso en sentido contrario -- un solo paso, no un bucle -- y queda visible
# en el confirmAfterWrite. Validar por sistema contra MSFS real antes de confiar.

- id: electrical.electrical_maint_sw
  # Maintenance Test (MAINT) Switch -- nodo VC_Electrical_Maint_SW del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 1), false = soltar (codigo 2).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_Electrical_Maint_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 1 (>L:VC_Electrical_trigger_VAL,number) } els{ 2 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.dc_meters_selector_sw
  # DC Meters Selector -- nodo VC_DC_Meters_Selector_SW del modelo iFly.
  # Selector/perilla de 70 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 3 sube, 4 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_DC_Meters_Selector_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_DC_Meters_Selector_SW_VAL,number) $value < if{ 3 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_DC_Meters_Selector_SW_VAL,number) $value > if{ 4 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.ac_meters_selector_sw
  # AC Meters Selector -- nodo VC_AC_Meters_Selector_SW del modelo iFly.
  # Selector/perilla de 70 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 5 sube, 6 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_AC_Meters_Selector_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_AC_Meters_Selector_SW_VAL,number) $value < if{ 5 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_AC_Meters_Selector_SW_VAL,number) $value > if{ 6 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.battery_sw
  # Battery (BAT) Switch -- nodo VC_Battery_SW del modelo iFly.
  # Selector/perilla de 10 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 8 sube, 7 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_Battery_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_Battery_SW_VAL,number) $value < if{ 8 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_Battery_SW_VAL,number) $value > if{ 7 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.cab_util_sw
  # CAB/UTIL Switch -- nodo VC_CAB_UTIL_SW del modelo iFly.
  # Selector/perilla de 10 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 9 sube, 10 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_CAB_UTIL_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_CAB_UTIL_SW_VAL,number) $value < if{ 9 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_CAB_UTIL_SW_VAL,number) $value > if{ 10 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.standby_power_sw
  # STANDBY POWER Switch -- nodo VC_STANDBY_POWER_SW del modelo iFly.
  # Selector/perilla de 20 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 11 sube, 12 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_STANDBY_POWER_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_STANDBY_POWER_SW_VAL,number) $value < if{ 11 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_STANDBY_POWER_SW_VAL,number) $value > if{ 12 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.idg_1_disc_sw
  # Generator Drive Disconnect (DISCONNECT) Switches -- nodo VC_IDG_1_Disc_SW del modelo iFly.
  # Selector/perilla de 10 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 13 sube, 14 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_IDG_1_Disc_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_IDG_1_Disc_SW_VAL,number) $value < if{ 13 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_IDG_1_Disc_SW_VAL,number) $value > if{ 14 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.idg_2_disc_sw
  # Generator Drive Disconnect (DISCONNECT) Switches -- nodo VC_IDG_2_Disc_SW del modelo iFly.
  # Selector/perilla de 10 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 15 sube, 16 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_IDG_2_Disc_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_IDG_2_Disc_SW_VAL,number) $value < if{ 15 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_IDG_2_Disc_SW_VAL,number) $value > if{ 16 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.ground_power_sw
  # Ground Power (GRD PWR) Switch -- nodo VC_Ground_Power_SW del modelo iFly.
  # Selector/perilla de 20 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 17 sube, 18 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_Ground_Power_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_Ground_Power_SW_VAL,number) $value < if{ 17 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_Ground_Power_SW_VAL,number) $value > if{ 18 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.bus_transfer_sw
  # BUS TRANSFER Switch -- nodo VC_Bus_Transfer_SW del modelo iFly.
  # Selector/perilla de 10 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 20 sube, 19 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_Bus_Transfer_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_Bus_Transfer_SW_VAL,number) $value < if{ 20 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_Bus_Transfer_SW_VAL,number) $value > if{ 19 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.eng_1_generator_sw
  # Generator (GEN) Switches -- nodo VC_ENG_1_Generator_SW del modelo iFly.
  # Selector/perilla de 20 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 21 sube, 22 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ENG_1_Generator_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_ENG_1_Generator_SW_VAL,number) $value < if{ 21 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_ENG_1_Generator_SW_VAL,number) $value > if{ 22 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.eng_2_generator_sw
  # Generator (GEN) Switches -- nodo VC_ENG_2_Generator_SW del modelo iFly.
  # Selector/perilla de 20 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 23 sube, 24 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ENG_2_Generator_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_ENG_2_Generator_SW_VAL,number) $value < if{ 23 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_ENG_2_Generator_SW_VAL,number) $value > if{ 24 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.apu_generator_1_sw
  # APU Generator (GEN) Switches -- nodo VC_APU_Generator_1_SW del modelo iFly.
  # Selector/perilla de 20 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 26 sube, 25 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_APU_Generator_1_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_APU_Generator_1_SW_VAL,number) $value < if{ 26 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_APU_Generator_1_SW_VAL,number) $value > if{ 25 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.apu_generator_2_sw
  # APU Generator (GEN) Switches -- nodo VC_APU_Generator_2_SW del modelo iFly.
  # Selector/perilla de 20 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 28 sube, 27 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_APU_Generator_2_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_APU_Generator_2_SW_VAL,number) $value < if{ 28 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_APU_Generator_2_SW_VAL,number) $value > if{ 27 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.ife_pass_seat_sw
  # IFE/PASS SEAT Switch -- nodo VC_IFE_PASS_SEAT_SW del modelo iFly.
  # Selector/perilla de 10 pasos. Escritura direccional: un solo
  # paso hacia el valor pedido por escritura (codigo 30 sube, 29 baja); si ya
  # esta en el destino no dispara nada. NO es un TOGGLE ciego.
  dataType: number
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_IFE_PASS_SEAT_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "(L:VC_IFE_PASS_SEAT_SW_VAL,number) $value < if{ 30 (>L:VC_Electrical_trigger_VAL,number) } (L:VC_IFE_PASS_SEAT_SW_VAL,number) $value > if{ 29 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 150

- id: electrical.electrical_light_grd_power_available
  # Ground Power (GRD PWR) Switch -- nodo VC_ELECTRICAL_LIGHT_GRD_POWER_AVAILABLE del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 31), false = soltar (codigo 32).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_GRD_POWER_AVAILABLE_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 31 (>L:VC_Electrical_trigger_VAL,number) } els{ 32 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_drive_1
  # Generator Drive (DRIVE) Lights -- nodo VC_ELECTRICAL_LIGHT_DRIVE_1 del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 33), false = soltar (codigo 34).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_DRIVE_1_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 33 (>L:VC_Electrical_trigger_VAL,number) } els{ 34 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_drive_2
  # Generator Drive (DRIVE) Lights -- nodo VC_ELECTRICAL_LIGHT_DRIVE_2 del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 35), false = soltar (codigo 36).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_DRIVE_2_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 35 (>L:VC_Electrical_trigger_VAL,number) } els{ 36 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_standby_pwr_off
  # STANDBY Power Off (PWR OFF) Light -- nodo VC_ELECTRICAL_LIGHT_STANDBY_PWR_OFF del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 37), false = soltar (codigo 38).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_STANDBY_PWR_OFF_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 37 (>L:VC_Electrical_trigger_VAL,number) } els{ 38 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_gen_off_bus_1
  # Generator Off Bus (GEN OFF BUS) Lights -- nodo VC_ELECTRICAL_LIGHT_GEN_OFF_BUS_1 del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 39), false = soltar (codigo 40).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_GEN_OFF_BUS_1_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 39 (>L:VC_Electrical_trigger_VAL,number) } els{ 40 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_gen_off_bus_2
  # Generator Off Bus (GEN OFF BUS) Lights -- nodo VC_ELECTRICAL_LIGHT_GEN_OFF_BUS_2 del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 41), false = soltar (codigo 42).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_GEN_OFF_BUS_2_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 41 (>L:VC_Electrical_trigger_VAL,number) } els{ 42 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_transfer_bus_off_1
  # TRANSFER BUS OFF Lights -- nodo VC_ELECTRICAL_LIGHT_TRANSFER_BUS_OFF_1 del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 43), false = soltar (codigo 44).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_TRANSFER_BUS_OFF_1_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 43 (>L:VC_Electrical_trigger_VAL,number) } els{ 44 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_transfer_bus_off_2
  # TRANSFER BUS OFF Lights -- nodo VC_ELECTRICAL_LIGHT_TRANSFER_BUS_OFF_2 del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 45), false = soltar (codigo 46).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_TRANSFER_BUS_OFF_2_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 45 (>L:VC_Electrical_trigger_VAL,number) } els{ 46 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_source_off_1
  # SOURCE OFF Lights -- nodo VC_ELECTRICAL_LIGHT_SOURCE_OFF_1 del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 47), false = soltar (codigo 48).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_SOURCE_OFF_1_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 47 (>L:VC_Electrical_trigger_VAL,number) } els{ 48 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_source_off_2
  # SOURCE OFF Lights -- nodo VC_ELECTRICAL_LIGHT_SOURCE_OFF_2 del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 49), false = soltar (codigo 50).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_SOURCE_OFF_2_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 49 (>L:VC_Electrical_trigger_VAL,number) } els{ 50 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_apu_gen_off_bus
  # APU Generator (GEN) Switches -- nodo VC_ELECTRICAL_LIGHT_APU_GEN_OFF_BUS del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 51), false = soltar (codigo 52).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_APU_GEN_OFF_BUS_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 51 (>L:VC_Electrical_trigger_VAL,number) } els{ 52 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_bat_discharge
  # Battery Discharge (BAT DISCHARGE) Light -- nodo VC_ELECTRICAL_LIGHT_BAT_DISCHARGE del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 53), false = soltar (codigo 54).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_BAT_DISCHARGE_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 53 (>L:VC_Electrical_trigger_VAL,number) } els{ 54 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_tr_unit
  # TR UNIT Light -- nodo VC_ELECTRICAL_LIGHT_TR_UNIT del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 55), false = soltar (codigo 56).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_TR_UNIT_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 55 (>L:VC_Electrical_trigger_VAL,number) } els{ 56 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.electrical_light_elec
  # Electrical (ELEC) Light -- nodo VC_ELECTRICAL_LIGHT_ELEC del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 57), false = soltar (codigo 58).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_ELECTRICAL_LIGHT_ELEC_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 57 (>L:VC_Electrical_trigger_VAL,number) } els{ 58 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50

- id: electrical.ground_service_sw
  # Ground Service Switch -- nodo VC_Ground_Service_SW del modelo iFly.
  # Boton momentaneo: true = pulsar (codigo 59), false = soltar (codigo 0).
  # Set explicito en ambos sentidos, no un TOGGLE.
  dataType: boolean
  authority: shared
  read:
    type: clientDataArea
    areaName: "SharedCockpitBridge_LVars"
    field: "L:VC_Ground_Service_SW_VAL"
    nativeType: float
  write:
    type: calculatorCode
    name: "$value 0 > if{ 59 (>L:VC_Electrical_trigger_VAL,number) } els{ 0 (>L:VC_Electrical_trigger_VAL,number) }"
  synchronization:
    mode: event
    confirmAfterWrite: true
    debounceMs: 50
